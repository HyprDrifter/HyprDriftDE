-- Per-device mouse configuration from Mouse.conf.
-- This file is not loaded by default because the old main config did not source it.
hl.device({
    name = "epic-mouse-v1",
    sensitivity = -0.9,
})
